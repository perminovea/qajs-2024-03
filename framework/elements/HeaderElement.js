// eslint-disable-next-line no-unused-vars
function HeaderElement({ page }) {
  const goHome = () => {
    //
  }

  const openPage = pageTitle => {
    //
  }

  return {
    goHome,
    openPage,
  }
}
